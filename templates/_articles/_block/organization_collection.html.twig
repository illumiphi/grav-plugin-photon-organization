{% import "_macros/collection.html.twig" as collection %}
{{  collection.render(page, '')  }}
{{  collection.render(page, 'members')  }}
{{  collection.render(page, 'events')  }}
{{  collection.render(page, 'posts')  }}
